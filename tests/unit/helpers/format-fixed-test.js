
import { formatFixed } from 'fuel-points/helpers/format-fixed';
import { module, test } from 'qunit';

module('Unit | Helper | format fixed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatFixed([42]);
  assert.ok(result);
});

