
import { empty } from 'fuel-points/helpers/empty';
import { module, test } from 'qunit';

module('Unit | Helper | empty');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = empty([42]);
  assert.ok(result);
});

